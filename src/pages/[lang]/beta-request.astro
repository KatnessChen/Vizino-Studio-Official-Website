---
import { ui } from '../../i18n/translations';

export function getStaticPaths() {
  return [{ params: { lang: 'en' } }, { params: { lang: 'zh-tw' } }];
}

const { lang } = Astro.params;
// Redirect old beta-request to waitlist
return Astro.redirect(`/${lang}/waitlist/`);
---
